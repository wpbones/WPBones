/node_modules
release.sh
vendor/
.idea/

# Announcement post (local only)
POST.md

# Tests: library, no lockfile; local PHPUnit overrides and cache stay out
/composer.lock
/phpunit.xml
/.phpunit.cache/
